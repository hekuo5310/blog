{
  "name": "blog",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --config wrangler.jsonc",
    "deploy": "wrangler deploy --config wrangler.jsonc",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && wrangler deploy --dry-run --config wrangler.jsonc"
  },
  "dependencies": {
    "hono": "^4.12.30",
    "pinyin-pro": "^3.25.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260716.1",
    "typescript": "^5.5.0",
    "wrangler": "^4.111.0"
  }
}
